When our app wants to integrate a AI inside of it, for a client or for any process we can make it through the SDK or a simple HTTP request to the model.

> [!warning] 
> This requests should be made in the backend, because each of these requests needs the API key to functioning properly
